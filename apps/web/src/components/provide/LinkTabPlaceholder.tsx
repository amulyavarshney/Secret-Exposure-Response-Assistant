export function LinkTabPlaceholder() {
  return (
    <div className="muted">
      <p>
        <strong>Link / connector import</strong> — coming in a future release.
      </p>
      <p>
        This tab will pull content from GitHub gists, Slack messages, or
        configured connectors without manual paste. For now, use Paste or Upload.
      </p>
    </div>
  );
}
